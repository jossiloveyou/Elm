import {} from '@/constants/actionTypes'
const defaultState = {
  data: [],
}

export default function home (state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}